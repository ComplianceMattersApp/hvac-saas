/**
 * Shared page-width templates for authenticated application surfaces.
 *
 * Operational workspaces use the widest standard shell. Individual reading
 * regions can still constrain their own line length inside this boundary.
 */
export const OPERATIONAL_WORKSPACE_MAX_WIDTH_CLASS = "max-w-[92rem]";

/** Directory, settings, and form-heavy pages that do not need a wide rail. */
export const STANDARD_PAGE_MAX_WIDTH_CLASS = "max-w-7xl";

